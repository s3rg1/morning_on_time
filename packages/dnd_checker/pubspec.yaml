name: dnd_checker
description: Checks Android Do Not Disturb status for Morning On Time
version: 0.0.1

environment:
  sdk: ^3.10.7
  flutter: '>=3.0.0'

flutter:
  plugin:
    platforms:
      android:
        package: com.lagunitacrew.dnd_checker
        pluginClass: DndCheckerPlugin

dependencies:
  flutter:
    sdk: flutter
